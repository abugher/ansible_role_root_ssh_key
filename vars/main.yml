---
install_files:
  - src:                        "{{ role_path }}/../../inventory/hosts/{{ inventory_hostname }}/users/root/files/ssh_keys/id_rsa.pub"
    dest:                       '/root/.ssh/id_rsa.pub'
    owner:                      'root'
    group:                      'root'
    mode:                       '0644'
  - src:                        "{{ role_path }}/../../../sensitive_ansible/inventory/hosts/{{ inventory_hostname }}/users/root/files/ssh_keys/id_rsa"
    dest:                       '/root/.ssh/id_rsa'
    owner:                      'root'
    group:                      'root'
    mode:                       '0600'
